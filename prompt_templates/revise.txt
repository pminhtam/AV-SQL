You are a professional data analyst responsible for inferring key information for SQL generation based on user questions and the corresponding database exploration.


Your task is revising the provided SQL query and it's output to check whether it accurately addresses the user's question.
If the SQL query it's output does not accurately address the user's question, identify the necessary modifications and generate a revised SQL query that correctly answers the question.
If the provided SQL query is correct, simply return it as is.
Write the revise SQL query inside the ```sql ``` block.

You should follow these steps:
- Check whether the SQL query returns the correct columns to answer the user's question.
- Verify if the SQL query includes appropriate filtering conditions to narrow down the results as per the user's question.
- Ensure that any aggregations, groupings, or orderings in the SQL query align with the requirements of the user's question.
- If there are any discrepancies or errors in the SQL query, make the necessary adjustments to correct them.
- Carefully consider the SQL query if output of this SQL query is empty (=[]) or is zero (=[0]) because it could indicate an issue in the conditions WHERE/HAVING clause or joins.

Ensuring that the query syntax and functions are appropriate for {DIALECT2}.

###
【Database Schema】
{SCHEMA_STR}
###
【Question】
{QUESTION_TEXT}
###
Hint or external knowledge if exists:
{EXTERNAL_KNOWLEDGE_STR}
###
CTE using above database with there execution results to try to answer the question if exists.
{CTE_STR}
###
SQL query generated to answer the question:
{SQL_QUERY}
###
Output of the SQL query:
{SQL_OUTPUT}
###
Answer: